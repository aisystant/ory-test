{
  "name": "ory-auth-demo",
  "version": "1.0.0",
  "description": "Demo app to test Ory OAuth2 authorization (same params as digital-twin-mcp)",
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
